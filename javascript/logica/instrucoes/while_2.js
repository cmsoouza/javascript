let quantidade = 0;

//indeterminada de repetições
while (Math.random() < 0.97) {
    quantidade++;
}
console.log('Qtde: ' + quantidade);

/**
 * Nessa instrução do 'while' percebemos ao criar na expressao o metodo 'Math.
 * radom ele vai gerar um numero de 0 a 1 que enquanto for menor que '0.97' ele
 * vai incrementar o valor de "quantidade". Agora quando o valor for "maior"
 * o nao para de ser executado e sai do laço.
 * 
 * Os valores executados informa quantos valores menores que '0.97' foram sortea
 * dos.
 */