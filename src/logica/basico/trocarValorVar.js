let a = 7;
let b = 94;

let temp = a;
a = b;
b = temp;

console.log(a);
console.log(b);

/**
 * anotações:
 * 1. Quando criamos uma nova variavel para armazenar os valores padroes das vari
 *    aveis iniciais, isso quer dizer;
 *     # Ao criarmos (temp = valor de a (7))
 *     # Assim "a = 94" e "b = temp = 7"
 * 
 */
