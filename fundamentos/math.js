const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)

console.log(area);
console.log(typeof Math);


/**
 * anotações
 * 1.   Math é um objeto embutido que tem propriedades e metodos para constantes
 *      e funções matematica. Nao é um objeto de função.
 * 
 * 2.   Algumas caracteristicas de (Math.pow());
 *      2.1 Uma função retorna a base elevada ao expoente power, ou seja, base (expoente)
 *      2.2 base (o numero da base)
 *      2.3 expoente (expoente usado para elevar a "base")
 */