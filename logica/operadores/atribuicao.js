let a = 7; //atribuindo o valor '7' a variavel 'a'
a = 5;//uma nova atribuição
a += 4;//adição mais uma atribuição

//console.log(a);
//atribuição aditiva
let x = 10;
x = x + 6;
x = x - 1;

let y = 20;
y += 10; //y = y + 10; (atenção!!!)
y -= 2;
y *= 2;
y /= 4;
console.log(x);
console.log(y);

//concatenando atribuições
let w = 'texto';
w += ', mais texto';
w += '!!!';
console.log(w);

/**
 * Dicas Importantes: OPERADORES DE ATRIBUIÇÃO
 * 1. O operador basico da atribuição é (=), que atribui o valor da direita ao operando
 *    da esquerda.
 * 2. Operações de atribuição aceita varios operadores, como operador atribuição igual(+=)
 * 3. Tambem é posição realizar concatenações em expressoes de atribuição (exemplo acima)
 */