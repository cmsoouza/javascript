let hora = 14;
let saudacao = hora >= 11 ? 'bom dia': 'boa tarde';//cond > expr1 > expr2

console.log(saudacao);

/**
 * Dicas importantes OPERADORES TERNARIO
 * 1. operador condicional (ternario) é o unico que possui tres operandos.
 * 2. é freguentemente uma ponte para declarações para instruções como 'if'.
 * 3. se a condição for 'true', ele retornará o primeiro valor, se nao, o segundo
 *    valor da expr2.
 */