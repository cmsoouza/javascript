/*sintaxe
if(condicao) {
    afirmação v;
} else {
    afirmação f;
}
*/

//ex1
if (true)
    console.log('Vai ser interpretado!!!');

if (false)
    console.log('Nao será interpretado!!!');//(condicao) declarada nao executa 

console.log('Fim!!!');

/**
 * anotações;
 * 1. if é uma (palavra - reservada) uma (condicional) que executa a afirmação, dentro
 *    do bloco, se a condição for "verdadeira".
 * 
 * 2. Uma expressao pode ser avaliada como "true" ou "false".
 * 
 * 3. Nessa estrutura percebemos (subordinação) de uma setença com a outra
 *    assim a (indentação) do codigo implica na estrutura da aplicação.
 */