let verdadeiro = true;
let falso = false;

if (verdadeiro) {
    console.log('Exec...');
} else
    console.log('Nao exec...');
if (falso) {
    console.log('Exec...');
} else {
    console.log('Nao exec...');
}

/**
 * anotações;
 * 1. condicional if é uma estrutura que executa a afirmaçao, dentro do bloco, se a condi
 *    ção for verdadeira. Caso falsa, executará as afirmações dentro do else.
 * 2. sintaxe;
 * 
 *    if(condicao) afirmacao1 [else afirmacao2]
 */