for(let controle = 1; controle <= 10; controle += 2) {
    console.log(controle);
}
console.log('Fim!!!');

/**
 * A instrução for cria um loop que consiste em tres expressoes separadas por
 * ";" e dentro de ().
 * 
 * Sua sintaxe for([inicializaçao]; [condição]; [expressao final])
 * 
 *                          [{declaração}]
 * 
 * Nessa extrutura observamos que o laço possui uma variavel que começa de '1'
 * vai ate 10 e quando é incrementada para '11' sai fora do laco deixando de ser
 * verdadeira chegando ao fim. interessante quando vc incrementa uma atribuição 
 * aditiva para cada loop pula dois numeros.
 */