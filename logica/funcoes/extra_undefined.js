let indefinido;
let naoENumero = 7;
let nulo = null;

console.log(indefinido);
console.log(indefinido + nulo);
console.log(naoENumero + nulo + 1);

/**
 * anotações:
 * 1. Uma variavel que nao possui um valor atribuido é "undefined".
 * 
 * 2. "null" é um literal, palavra propria que colocamos no meio do codi
 *     go. Null é nulo = vazio (explicitamente). 
 */